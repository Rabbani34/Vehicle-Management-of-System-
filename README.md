# Vehicle-Management-of-System-
The Vehicle Management System is a Java OOP project that models different types of vehicles using inheritance and polymorphism. A base class (Vehicle) defines common attributes and behaviors, while child classes like Car, Motorcycle, and Truck extend it and provide their own implementations. The system demonstrates method overriding, abstract class
